# 06 — Дорожная карта и подэтапы реализации

## Структура фаз

```
Фаза 0: Данные → Фаза 1: Инфраструктура → Фаза 2: Каталог →
→ Фаза 3: Формы и заявки → Фаза 4: Франшиза → Фаза 5: FAQ →
→ Фаза 6: Сравнение → Фаза 7: Парсер-автомат → Фаза 8: Запуск
```

Каждый подэтап заканчивается работающим и тестируемым состоянием.
Переход к следующему — только после прохождения чеклиста текущего.

---

## Фаза 0 — Данные и решения

> Исторически это была предпроектная фаза. Сейчас ниже зафиксирован уже подтверждённый результат анализа.

### 0.1 Анализ сайта
- [x] Открыть altairika.ru, зафиксировать тип контента каталога: фильмы.
- [x] Записать поля карточки: название, описание, изображение, цена, возраст, продолжительность, ссылка.
- [x] Проверить технический способ загрузки: каталог приходит через Tilda Store API, а не в виде готовых HTML-карточек.
- [x] Определить структуру источника: страница `catalog_full` содержит блоки `recid/storepart`, товары подтягиваются через `/api/getproductslist/`.

**Выход:** зафиксированная структура каталога, список всех полей карточки.

### 0.2 Данные каталога
- [x] Зафиксировать текущий автоматический источник данных: канонический блок каталога на 166 фильмов.
- [x] Оценить количество позиций в каталоге: 166.
- [x] Принять решение: основной сценарий — ручной запуск парсера Tilda API.

### 0.3 Данные для остальных разделов
- [ ] Получить от заказчика: условия франшизы, FAQ, текст питча.
- [ ] Определить список конкурентов и параметры сравнения.
- [ ] Получить политику конфиденциальности и текст согласия на ПД.
- [ ] Согласовать тон и приветственный текст бота.

### 0.4 Технические решения
- [ ] Зарегистрировать бота в BotFather, получить токен.
- [ ] Выбрать хостинг (VPS / managed).
- [ ] Решить: Telegram ID администратора для уведомлений о заявках.

**Критерий завершения:** все данные собраны, неизвестных нет, можно начинать код.

---

## Фаза 1 — Инфраструктура

### 1.1 Структура проекта
- [ ] Создать файловую структуру (`bot/`, `handlers/`, `services/`, `repositories/`, `models/`, `keyboards/`, `states/`).
- [ ] Настроить `config.py`: загрузка из `.env` (токен бота, DSN базы данных, Telegram ID администратора).
- [ ] Добавить `.env.example`, `.gitignore`.

**Проверка:** `python -m bot` запускается без ошибок (пустой бот).

### 1.2 База данных
- [ ] Написать модели SQLAlchemy (все таблицы из документа 04).
- [ ] Настроить Alembic: `alembic init`, первая миграция.
- [ ] Проверить применение миграций на чистой БД.

**Проверка:** `alembic upgrade head` проходит без ошибок, таблицы созданы.

### 1.3 Docker
- [ ] Написать `Dockerfile` для бота.
- [ ] Написать `docker-compose.yml` (бот + PostgreSQL).
- [ ] Проверить запуск всего стека через `docker-compose up`.

**Проверка:** контейнеры запускаются, бот видит БД, миграции применяются при старте.

### 1.4 Базовый бот
- [ ] Handler `/start` → логирует пользователя в `BotUser`, отвечает «Привет».
- [ ] Handler `/menu` → то же сообщение.
- [x] Подключить polling.

**Проверка:** бот отвечает на `/start` в Telegram, запись в `BotUser` создаётся.

---

## Фаза 2 — Каталог

### 2.1 Загрузка данных каталога
- [x] Написать скрипт автосинхронизации каталога через Tilda API.
- [x] Загрузить реальные данные каталога в БД.
- [x] Проверить корректность полей, особенно `image_url`, `duration`, `age_rating`.

**Проверка:** в таблице `CatalogItem` есть 166 реальных позиций, `Category` заполнена одной рабочей категорией `Каталог`.

### 2.2 Главное меню
- [ ] Отправить главное меню при `/start` и `/menu`.
- [ ] Inline-кнопки: Каталог, Франшиза, Частые вопросы, Контакт.
- [ ] Нажатие любой кнопки — редактирование текущего сообщения (не новое).

**Проверка:** кнопки меню работают, сообщение редактируется, в чате не появляется мусор.

### 2.3 Список категорий
- [ ] Экран категорий формируется динамически из `Category`.
- [ ] Кнопка «Все» + кнопка на каждую категорию.
- [ ] Если категория одна — пропустить экран, сразу список.
- [ ] Кнопка «← Главное меню».

**Проверка:** при изменении данных в `Category` кнопки обновляются без перезапуска.

### 2.4 Список карточек с пагинацией
- [ ] 5 позиций на страницу.
- [ ] Кнопки пагинации: `← / Страница N из M / →`.
- [ ] Кнопки открытия карточек под списком.
- [ ] Кнопка «← Назад к категориям».
- [ ] Всё через редактирование текущего сообщения.

**Проверка:** пагинация работает, состояние страницы сохраняется при нажатии «←/→».

### 2.5 Карточка без изображения
- [ ] Полный текст карточки: название, описание, категория, цена, длительность (пустые поля не показываются).
- [ ] Кнопки: «Записаться», «← Назад к списку», «Похожие» (если есть другие в категории).
- [ ] Редактирование текущего сообщения.

**Проверка:** все поля карточки отображаются корректно, пустые поля отсутствуют в тексте.

### 2.6 Карточка с изображением
- [ ] Карточка с `image_url` → отправляется как `sendPhoto` с caption.
- [ ] Caption ≤ 1024 символов; если длиннее — усечение с `...` и кнопкой «Читать подробнее».
- [ ] Те же кнопки действий.
- [ ] «← Назад» из карточки с фото → новое текстовое сообщение со списком (фото остаётся в истории — это нормально).

**Проверка:** карточки с постером корректно отображаются на iOS и Android.

---

## Фаза 3 — Формы и заявки

### 3.1 FSM-форма: запись на сеанс
- [ ] Состояния: `waiting_name` → `waiting_phone` → `waiting_time` → `confirm`.
- [ ] Шаг «время» — с кнопкой «Пропустить».
- [ ] Запоминать, из какой карточки запущена форма (передавать `catalog_item_id`).

### 3.2 Шаг «телефон»
- [ ] Кнопка «Поделиться контактом» (нативная, verifies Telegram номер).
- [ ] Текстовый ввод как резервный вариант.
- [ ] Базовая валидация номера (только цифры, длина 10–15, опциональный `+`).
- [ ] Сообщение об ошибке при некорректном вводе.

### 3.3 Шаг подтверждения
- [ ] Показать все введённые данные перед сохранением.
- [ ] Кнопки: «Всё верно, отправить» / «Изменить».
- [ ] «Изменить» → возврат к шагу 1 с сохранёнными данными.
- [ ] Добавить текст согласия на ПД с ссылкой на политику.

### 3.4 Сохранение и уведомление
- [ ] После подтверждения → создать `Lead` в БД.
- [ ] Отправить уведомление администратору (Telegram ID из конфига).
- [ ] Защита от двойного сохранения (двойное нажатие кнопки).
- [ ] Финальное сообщение пользователю с кнопками навигации.

**Проверка:** заявка появляется в БД, администратор получает уведомление, двойное нажатие не создаёт дубль.

### 3.5 Быстрая форма «Связаться»
- [ ] Из главного меню → форма без привязки к карточке (`lead_type = contact`).
- [ ] Те же шаги: имя, телефон, подтверждение.
- [ ] Команда `/contact`.

### 3.6 Обработка прерывания формы
- [ ] Нажатие inline-кнопки меню в середине формы → «Продолжить оформление или выйти?».
- [ ] `/start` в середине формы → сбросить FSM, начать заново.

**Проверка:** все краевые случаи из документа 02 обработаны.

---

## Фаза 4 — Раздел франшизы

### 4.1 Главная страница франшизы
- [ ] Текст питча из `FranchiseContent` (секция `pitch`).
- [ ] Меню подразделов: Условия, Поддержка, Рынок и конкуренты, FAQ, Оставить заявку.

### 4.2 Подразделы
- [ ] «Условия и инвестиции» → текст из `FranchiseContent` (секция `conditions`).
- [ ] «Поддержка и обучение» → текст из `FranchiseContent` (секция `support`).
- [ ] «Частые вопросы» → переиспользовать FAQ (тема «О франшизе»).
- [ ] Каждый подраздел: кнопки «← Назад» и «Оставить заявку».

### 4.3 Форма заявки на франшизу
- [ ] Те же шаги что в 3.1–3.4, плюс шаг «Город / регион».
- [ ] `lead_type = franchise`.
- [ ] Уведомление администратору с пометкой «ФРАНШИЗА».

**Проверка:** все подразделы отображаются корректно, заявка сохраняется с типом `franchise`.

---

## Фаза 5 — FAQ

### 5.1 Загрузка данных FAQ
- [x] Создать темы и вопросы в БД (из согласованных материалов заказчика).
- [x] Добавить скрипт-наполнитель.

### 5.2 Навигация по FAQ
- [ ] Экран тем (`FaqTopic`) — динамически из БД.
- [ ] Экран вопросов темы — кнопка на каждый вопрос.
- [ ] Экран ответа — текст + «← Назад к вопросам» + «📞 Написать свой вопрос».
- [ ] Всё через редактирование сообщения.

### 5.3 «Написать свой вопрос» (вопрос оператору)
- [ ] Пользователь пишет текст → сохраняется в `UserQuestion`.
- [ ] Администратор получает уведомление с текстом вопроса.
- [ ] Ответ бота: «Спасибо! Мы ответим в ближайшее рабочее время.»

### 5.4 Обработка свободного текста
- [ ] Любой текст вне активной формы → ответ с кнопками «Частые вопросы» и «Написать вопрос».
- [ ] Никакого разбора намерений.

**Проверка:** FAQ проходится от темы до ответа; свободный текст не ломает бота.

---

## Фаза 6 — Сравнение с конкурентами

### 6.1 Загрузка данных
- [x] Заполнить `Competitor`, `ComparisonParameter`, `ComparisonValue` начальными данными через сиды.

### 6.2 Экран сравнения
- [ ] Таблица или список: Altairika vs конкуренты по параметрам.
- [ ] Иконки ✅/⚠️/❌ по полю `rating`.
- [ ] Сноска с датой актуальности данных.
- [ ] Доступен из: раздел франшизы → «Рынок и конкуренты».
- [ ] Кнопки: «← Назад», «📞 Записаться», «❓ FAQ».

**Проверка:** таблица читается на узком экране мобильного (если нет — переверстать в список).

---

## Фаза 7 — Автоматический парсер

> Техническая возможность подтверждена: текущий сайт отдаёт каталог через Tilda Store API.

### 7.1 Парсер
- [x] Реализовать скрипт парсинга через Tilda API.
- [x] Добавить нормализацию и валидацию данных перед записью.
- [x] Добавить логику синхронизации: новые → добавить, изменённые → обновить, удалённые → `is_active = false`.

### 7.2 Планировщик
- [x] Убрать автозапуск парсинга из бота, оставить ручной запуск.
- [x] Настроить запуск 1 раз в сутки ночью.
- [ ] Инвалидация кэша категорий и списков после парсинга.
- [x] Добавить логирование результатов: добавлено X, обновлено Y, деактивировано Z.

**Проверка:** парсер отрабатывает без ошибок на реальном сайте и загружает в БД 166 позиций каталога.


---

## Таблица зависимостей между фазами

| Фаза | Зависит от |
|------|-----------|
| 1 (Инфраструктура) | 0 (все данные собраны) |
| 2 (Каталог) | 1 + данные каталога (0.1, 0.2) |
| 3 (Формы) | 1 + 2.2 (главное меню) |
| 4 (Франшиза) | 1 + данные франшизы (0.3) |
| 5 (FAQ) | 1 + данные FAQ (0.3) |
| 6 (Сравнение) | 1 + данные конкурентов (0.3) |
| 7 (Парсер-автомат) | 2 (каталог работает) |

ка
---

## Критерии готовности (DoD) всего проекта

- [ ] Пользователь проходит путь от /start до сохранённой заявки без помощи.
- [ ] Оператор видит все заявки в Telegram-уведомлениях.
- [ ] Бот не падает при любом вводе пользователя.
- [ ] Навигация работает через редактирование сообщений (чат не засоряется).
- [ ] Карточки с изображениями отображают постер.
- [ ] FAQ проходится полностью.
- [ ] Webhook работает на продакшн-сервере.
- [ ] Данные резервируются.

---

## Риски

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Сайт изменит структуру | Средняя | Обновить ручной парсер Tilda API или внести данные напрямую в БД |
| Заказчик не предоставил тексты | Средняя | Не начинать Фазы 4 и 5 без данных |
| Таблица конкурентов нечитаема на мобильном | Низкая | Переверстать в список — простое исправление |
| Дубли заявок при двойном нажатии | Средняя | Решается в Фазе 3.4 — идемпотентность через уникальный ключ |
